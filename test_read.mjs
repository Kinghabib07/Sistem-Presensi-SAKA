import { initializeApp } from "firebase/app"; import { getAuth, signInWithEmailAndPassword } from "firebase/auth"; import { getDatabase, ref, get, child } from "firebase/database"; const firebaseConfig = { apiKey: "AIzaSyDUgyqv7BydIj-UuvteraWyNm1utea2KUI", authDomain: "surat-izin-sma.firebaseapp.com", databaseURL: "https://surat-izin-sma-default-rtdb.asia-southeast1.firebasedatabase.app", projectId: "surat-izin-sma", storageBucket: "surat-izin-sma.firebasestorage.app", messagingSenderId: "730621657610", appId: "1:730621657610:web:be0e204f8ea12ddecf55f3" }; const app = initializeApp(firebaseConfig); const auth = getAuth(app); const db = getDatabase(app); signInWithEmailAndPassword(auth, "admin@sekolah.id", "admin123").then((cred) => { console.log("Logged in as:", cred.user.uid); get(child(ref(db), "users")).then((snapshot) => { if (snapshot.exists()) { console.log("Users:", Object.keys(snapshot.val())); } else { console.log("No data available"); } get(child(ref(db), "presensi")).then((snap2) => { console.log("Presensi:", snap2.exists() ? Object.keys(snap2.val()) : "Empty"); process.exit(0); }).catch(e => { console.log("Presensi Error:", e.message); process.exit(1); }); }).catch(e => { console.log("Users Error:", e.message); process.exit(1); }); }).catch(e => { console.log("Login Error:", e.message); process.exit(1); });
